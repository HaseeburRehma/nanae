// This file is intentionally unused — InstagramTestimonials is imported
// directly in src/app/page.tsx because it already has its own client-side
// `mounted` guard, so the dedicated loader wrapper is redundant.
//
// Kept (empty) to avoid breaking any stray imports during the transition.
// Safe to delete in a future cleanup.

export {};
